\chapter{Inversione delle mappe di flusso}



 \section{Introduzione}
L'inversione delle mappe di flusso è un passaggio fondamentale per implementare il modello su Simulink.\\
Nelle mappe del motore di Test il flusso D e il flusso Q vengono espressi in funzione della corrente D, della corrente Q e della posizione meccanica. Tramite l'inversione si esprime la corrente D e Q in 
funzione dei due flussi e della posizione meccanica.\\ 
Per invertire il flusso sono state adoperate due metodologie distinte:
\begin{enumerate}
\item Minimizzazione di una funzione di costo.\\
La funzione di costo da minimizzare ha la seguente forma:
\begin{equation}
    J(\mathbf{i}_{dq}) = || \lambda_d^*(k) - \lambda_d(\mathbf{i}_{dq}, \theta) || + || \lambda_q^*(k) - \lambda_q(\mathbf{i}_{dq}, \theta) ||
\end{equation}
I vettori di flusso che costituiranno le nuove coordinate della mappa vengono generati usando la funzione "linspace", creando un range di punti equispaziati tra i due limiti  imposti.\\
Tale funzione permette di trovare la coppia di correnti  $i_d$ e $i_q$ il cui flusso associato è quello più prossimo al flusso di target.
\item Inversione della griglia attraverso dati sparsi: 
Questo metodo è significativamente più rapido rispetto al precedente. L'inversione avviene passando alla funzione  "scatteredInterpolant" la terna di valori costituita dai flussi d e q e dalla corrente $i_d$ o $i_q$. Passando alla funzione interpolatore i valori appartenenti al range di flusso prestabilito si ricostruisce la mappa inversa.
\end{enumerate}

\subsection{Scelta degli estremi del dominio del flusso}
\label{sec:limiti_dominio_flusso}
È fondamentale definire con cautela gli estremi del dominio dei flussi per la mappa invertita. Infatti, l'utilizzo di un intervallo troppo esteso comporterebbe la presenza di punti (coppie di flusso e posizione angolare) a cui non è associata alcuna coppia di correnti. Ciò accade perché tali valori eccedono il flusso massimo della mappa diretta, uscendo così dal dominio di interpolazione.\\
Per ovviare a questo problema il limite superiore del dominio del flusso è stato imposto come il \textit{minimo tra i valori massimi} di flusso per ogni posizione angolare. Seguendo lo stesso criterio, il limite inferiore  è stato imposto come il \textit{massimo tra i valori minimi} di flussi per ogni posizione angolare:



\begin{equation}
\begin{cases}
    \lambda_{d,max}^\text{Dom}  = \min_{\theta_m} \left( \max(\lambda_{d}(\theta_m)) \right) \\[10pt]
    \lambda_{d,min}^\text{Dom} = \max_{\theta_m} \left( \min(\lambda_{d}(\theta_m)) \right)
\end{cases}
\end{equation}

e analogamente per $\lambda_q$:

\begin{equation}
\begin{cases}
    \lambda_{q,max}^\text{Dom}  = \min_{\theta_m} \left( \max(\lambda_{q}(\theta_m)) \right) \\[10pt]
    \lambda_{q,min}^\text{Dom}  = \max_{\theta_m} \left( \min(\lambda_{q}(\theta_m)) \right)
\end{cases}
\end{equation}

Nel caso del motore di test, i range $\lambda_d$ e $\lambda_q$ presentano i seguenti valori estremi:
\begin{equation}
\begin{cases}
    \lambda_{d,min}^\text{Dom}  =  0.0567\ T \\[10pt]
    \lambda_{d,max}^\text{Dom}  =  0.2066\ T
\end{cases}
\qquad \text{e} \qquad
\begin{cases}
    \lambda_{q,min}^\text{Dom}  =  -0.5561\ T \\[10pt]
    \lambda_{q,max}^\text{Dom}  =  0.5559\ T
\end{cases}
\end{equation}
\subsection{Modello alternativo del motore}
Per evitare di invertire le mappe di flusso, si possono utilizzare le induttanze differenziali.\\
Si possono ottenere calcolando il gradiente di $\lambda_d$ e $\lambda_q$ rispetto alle tre dimensioni: $i_d$,$i_q$ e $\theta_m$. 

\subsection{Considerazioni sull'accuratezza della mappa invertita}
Per verificare l'accuratezza della mappa ottenuta tramite inversione si può seguire la seguente procedura:

    \begin{enumerate}
    \item \textbf{Generazione dei vettori di test}: vengono definiti due vettori contenenti valori di test per i flussi $\lambda_d$ e $\lambda_q$. Per generare tali vettori, sono state usate le funzioni \texttt{logspace} e \texttt{linspace}, assicurandosi che i punti di test non coincidano con i nodi della griglia originale.
    \item \textbf{Calcolo delle correnti corrispondenti}: attraverso le mappe invertite e l'algoritmo di interpolazione lineare (\texttt{interpn}), si determinano i valori di corrente $i_d$ e $i_q$ associati a ciascuna coppia di flussi di test.
    \item \textbf{Ritorno al valore di flusso}:  tramite le mappe dirette e la funzione "\texttt{interpn}" si calcolano i valori di flusso associati della mappa iniziale.
    \item \textbf{Confronto tra i flussi ottenuti}: infine si calcola l'errore sottraendo ai flussi del vettore di test i flussi trovati nel passo precedente. L'errore può così essere analizzato con l'ausilio di un grafico.
    \end{enumerate}

\subsection{Analisi dei risultati}
Dall'analisi dei risultati ottenuti tramite il confronto dei flussi, si nota la presenza di un errore consistente (Figure \ref{fig:errore_relativo_mappe_flusso_D_xcorrenti_DEPT} e \ref{fig:errore_relativo_mappe_flusso_Q_xcorrenti_DEPT}). 

\begin{figure}
\centering
    \includegraphics[height=8 cm]{immagini/errori_mappa_flusso/relativo/errore_relativo_mappe_flusso_D_xcorrenti_DEPT.pdf}
    \caption{Mappa dell'errore relativo su $\lambda_d$.}
    \label{fig:errore_relativo_mappe_flusso_D_xcorrenti_DEPT}
\end{figure}


\begin{figure}
\centering
    \includegraphics[height=8 cm]{immagini/errori_mappa_flusso/relativo/errore_relativo_mappe_flusso_Q_xcorrenti_DEPT.pdf}
    \caption{Mappa dell'errore relativo su $\lambda_q$.}
    \label{fig:errore_relativo_mappe_flusso_Q_xcorrenti_DEPT}
\end{figure}

Per diminuire tale errore si è infittita artificialmente la griglia di corrente delle mappe di flusso dirette. In questo modo l'input della funzione di inversione ha più dati disponibili. Si è osservato come ciò migliora considerevolmente l'accuratezza dell'inversione (Figure \ref{fig:errore_relativo_mappe_flusso_D_xcorrenti_dense} e \ref{fig:errore_relativo_mappe_flusso_Q_xcorrenti_dense}). I flussi sono stati confrontati rispetto alla stessa posizione angolare, $\theta_m$ = 49°.

\begin{figure}
\centering
    \includegraphics[height=8 cm]{immagini/errori_mappa_flusso/relativo/errore_relativo_mappe_flusso_D_xcorrenti_dense.pdf}
    \caption{Mappa dell'errore relativo su $\lambda_d$.}
    \label{fig:errore_relativo_mappe_flusso_D_xcorrenti_dense}
\end{figure}


\begin{figure}
\centering
    \includegraphics[height=8 cm]{immagini/errori_mappa_flusso/relativo/errore_relativo_mappe_flusso_Q_xcorrenti_dense.pdf}
    \caption{Mappa dell'errore relativo su $\lambda_q$.}
    \label{fig:errore_relativo_mappe_flusso_Q_xcorrenti_dense}
\end{figure}

\begin{figure}
\centering
    \includegraphics[height=8 cm]{immagini/errori_mappa_flusso/mappe_griglia/Mappa_correnti_UnicoColore_dense.pdf}
    \caption{Rappresentazione grafica del codominio di corrente.}
    \label{fig:Mappa_lambdaD_UnicoColore_dense.pdf}
\end{figure}

Per cercare di ridurre ulteriormente l'errore si è provato ad aumentare il range del dominio del flussi $d$ e $q$ della mappa invertita (Figure \ref{fig:errore_relativo_mappe_flusso_D_xcorrenti_ext} e \ref{fig:errore_relativo_mappe_flusso_Q_xcorrenti_ext}). Tuttavia, ciò aumenta notevolmente l'errore per correnti vicine allo zero. La ragione di questo comportamento, come già discusso nel Paragrafo \ref{sec:limiti_dominio_flusso}, è che il codominio di corrente risultante non comprende i valori di corrente prossimi a zero. \\
Ciò risulta evidente nella Figura \ref{fig:Mappa_lambdaD_UnicoColore_dense.pdf}.






Si è provato ad estendere quindi il dominio del flusso oltre i limiti imposti in precedenza. Tuttavia, questa procedura comporta notevoli errori di stima vicino allo zero (Figura \ref{fig:errore_relativo_mappe_flusso_D_xcorrenti_ext}). Il motivo è che in questo modo si introducono delle coppie di valori di flusso d-q che non sono presenti nelle mappe dirette. L'interpolazione genera quindi dei valori errati di flusso.

\begin{figure}
\centering
    \includegraphics[height=8 cm]{immagini/errori_mappa_flusso/relativo/errore_relativo_mappe_flusso_D_xcorrenti_ext.pdf}
    \caption{Mappa dell'errore relativo su $\lambda_d$.}
    \label{fig:errore_relativo_mappe_flusso_D_xcorrenti_ext}
\end{figure}


\begin{figure}
\centering
    \includegraphics[height=8 cm]{immagini/errori_mappa_flusso/relativo/errore_relativo_mappe_flusso_Q_xcorrenti_ext.pdf}
    \caption{Mappa dell'errore relativo su $\lambda_q$.}  \label{fig:errore_relativo_mappe_flusso_Q_xcorrenti_ext}
\end{figure}

Per cercare di ridurre l'errore si è espanso il dominio della mappa di corrente diretta anche per valori positivi di $i_d$. In questa maniera gli estremi del dominio del flusso calcolati come esposto nel Paragrafo \ref{sec:limiti_dominio_flusso}. Come si può notare in questo modo il codominio di corrente copre anche i valori intorno allo 0.

\begin{figure}
\centering
    \includegraphics[height=8 cm]{immagini/errori_mappa_flusso/mappe_griglia/Mappa_correnti_UnicoColore_positive.pdf}u
    \caption{Rappresentazione grafica del codominio di corrente.}
    \label{fig:Mappa_lambdaD_UnicoColore_positive}
\end{figure}


\begin{figure}
\centering
    \includegraphics[height=8 cm]{immagini/errori_mappa_flusso/relativo/errore_relativo_mappe_flusso_D_xcorrenti_positive.pdf}
    \caption{Mappa dell'errore relativo su $\lambda_d$.}
    \label{fig:errore_relativo_mappe_flusso_D_xcorrenti_positive}
\end{figure}


\begin{figure}
\centering
    \includegraphics[height=8 cm]{immagini/errori_mappa_flusso/relativo/errore_relativo_mappe_flusso_Q_xcorrenti_positive.pdf}
    \caption{Mappa dell'errore relativo su $\lambda_q$.}  \label{fig:errore_relativo_mappe_flusso_Q_xcorrenti_positive}
\end{figure}

